以下是去豆包生成 PPT 的完整提示词，按幻灯片顺序排列，你直接复制每一条到豆包就能用。

================================================================================
【总体说明】
PPT 风格：简洁科技风，主色调用深绿 + 棕色（对应网站 Logo 配色），白底
页数：10-12 页
演示场合：毕业答辩
================================================================================


幻灯片 1 — 封面

提示词：
"帮我生成一页毕业答辩 PPT 封面，标题是《数问（Sown）数学社区网站设计与实现》，副标题写'信息安全技术应用专业 · 王旭'，右下角写'指导老师：王崎'。主色调用深绿色和棕色，简洁科技风，白底。标题字体要大、醒目。"


幻灯片 2 — 目录

提示词：
"帮我生成一页 PPT 目录页，列出以下 5 个章节：1. 项目背景与定位  2. 数据库设计  3. 核心功能与 AI 智能体  4. 安全防护与渗透测试  5. 总结与展望。排版简洁，用深绿色作为章节编号的颜色。"


幻灯片 3 — 项目背景与定位

提示词：
"帮我生成一页 PPT，标题为'项目背景与定位'。内容分三个要点：
1. 定位：面向中学生的垂直数学社区，品牌名'数问'谐音'数学提问'，英文 Sown 意为'播种'
2. 技术栈：原生 PHP + MySQL + 原生 JavaScript，未使用框架；Quill 富文本编辑器 + KaTeX/MathJax 公式渲染
3. 部署环境：Ubuntu 24.04 + Nginx + PHP 8.3 + MySQL 5.7，2GB 内存服务器
每点用图标装饰，不要大段文字，用关键词短语。"


幻灯片 4 — 整体代码结构

提示词：
"帮我生成一页 PPT，标题为'整体代码结构'。内容用分层结构图展示：
- 根目录：每个 PHP 文件对应一个页面（index.php 首页、forum.php 社区、post.php 帖子详情等）
- app/ 核心库：db.php（PDO 连接）、csrf.php（CSRF 防护）、auth.php（用户认证）、helpers.php（XSS 净化/工具函数）、ratelimit.php（频率限制）等 18 个文件
- partials/ 公共模板：header.php、footer.php 等
用层级箭头或树状图展示，不要大段文字。"


幻灯片 5 — 数据库 ER 图 + 表结构

提示词：
"帮我生成一页 PPT，标题为'数据库设计 —— ER 图与核心表'。左侧放一张简化的 ER 图（实体关系：user - post - comment - tag 之间的关联），右侧用表格列出核心表的关键字段：
- user 表：id, username, email, password(bcrypt), status, role, points
- post 表：id, user_id, title, content, like_count(反范式化), comment_count, favorite_count, review_status
- comment 表：id, post_id, user_id, parent_id(实现多级回复)
- post_like 表：(post_id, user_id) 联合唯一索引防重复点赞
底部加一条备注：'遵循第三范式，高频计数反范式化冗余存储，事务保证一致性'。"


幻灯片 6 — 索引设计

提示词：
"帮我生成一页 PPT，标题为'索引设计原理'。内容用三栏卡片布局：
卡片1——唯一索引（业务约束）：user(username/email/phone)、point_ledger(user_id,reason_key)、post_like(post_id,user_id)
卡片2——单列索引（加速查询）：post(user_id)、post(created_at)、post(status)、comment(post_id)
卡片3——联合索引（复合查询）：idx_post_status_created(status+created_at，覆盖社区列表排序)、notification(user_id+is_read+created_at，未读统计)
底部备注：'字段顺序遵循最左前缀原则，title 字段使用前缀索引节省空间'。"


幻灯片 7 — 核心功能逻辑

提示词：
"帮我生成一页 PPT，标题为'核心功能逻辑'。用四个横向模块展示：
模块1——发帖流程：CSRF验证 → XSS过滤 → 违规词检测 → 图片提取 → 标签关联 → 非管理员进入待审核
模块2——社区推流：推荐(最新)、热门(加权热度公式：点赞×2+评论+收藏 / (天数+1)，限30天内)、关注(JION关注用户)
模块3——积分系统：每日签到(复合主键防重复)、连续奖励(7天30分/30天120分)、里程碑奖励、FOR UPDATE行锁防超支
模块4——画板功能：Canvas API、贝塞尔曲线、函数图像绘制、50步撤销重做
每个模块用图标标识，文字精炼。"


幻灯片 8 — AI 智能体"应老师"

提示词：
"帮我生成一页 PPT，标题为'AI 智能体 —— 应老师（RAG 架构）'。内容分左右两部分：
左侧展示架构流程图（从上到下）：用户提问 → MiniMax Embedding API(1536维向量) → 余弦相似度检索 → teacher_knowledge知识库 → Top3匹配 → 拼接persona人设 → MiniMax M2.7大模型 → 生成回答
右侧用文字说明三个关键点：
1. 知识库来源：初中数学教材Markdown文件，按章节切块存储
2. 检索+生成：向量检索保证知识准确性，大模型保证回答自然
3. 降级策略：API余额不足时自动切换为关键词匹配+模板回复
整体强调'检索增强生成，回答有据可依'。"


幻灯片 9 — 渗透测试

提示词：
"帮我生成一页 PPT，标题为'渗透测试结果'。用表格展示测试项目和结果：
| 测试项 | 工具/方法 | 结果 |
| SQL注入 | sqlmap --level=3 --risk=2 | 无漏洞（全站PDO预处理，禁用模拟预处理） |
| XSS | <script> <img onerror> <svg onload> 等payload | 全部拦截（四层纵深防御） |
| CSRF | 外部页面自动提交表单 | 全部403（32位随机令牌+hash_equals验证） |
| 文件上传 | .php/.asp/.exe/修改后缀/图片马 | 全部拦截（MIME检测+白名单+重命名） |
| 暴力破解 | Hydra + 自定义脚本 | 5次失败锁定15分钟，bcrypt减慢破解 |
| 越权/目录遍历 | ../路径穿越/越权访问接口 | 全部拒绝 |
底部结论：'经测试，系统无高危漏洞'。"


幻灯片 10 — 漏洞防护思路

提示词：
"帮我生成一页 PPT，标题为'安全防护体系（纵深防御）'。用六个竖向模块依次排列：
1. SQL注入 → PDO预处理 + ATTR_EMULATE_PREPARES=false
2. XSS → 四层防御：htmlspecialchars输出转义 → DOMDocument白名单过滤 → CSP头 → HttpOnly Cookie
3. CSRF → 同步器令牌模式 + SameSite=Lax
4. 认证安全 → bcrypt密码哈希 + 5次失败锁定15分钟
5. 文件上传 → finfo_file检测真实MIME + 重命名 + 剥离EXIF + 目录禁止执行PHP
6. 频率限制 → 文件系统滑动窗口（点赞30次/分钟、登录10次/分钟、注册5次/5分钟）
每个模块左侧放一个安全图标，右侧用短句描述。整体用绿色盾牌风格装饰。"


幻灯片 11 — 总结与展望

提示词：
"帮我生成一页 PPT，标题为'总结与展望'。分左右两栏：
左栏——已完成：原生PHP完整社区系统、20张表数据库设计、RAG架构AI问答、全站安全防护体系、渗透测试验证无高危漏洞
右栏——待改进：引入Elasticsearch或MySQL全文索引优化搜索、接入云端内容安全API提升审核精度、高并发压测与性能优化
底部居中写'谢谢 请各位老师批评指正'。"

================================================================================
【使用建议】
1. 把豆包生成的 PPT 下载为 PPTX 文件
2. 打开后可能需要微调排版（字体大小、图标位置、颜色统一）
3. PPT 中涉及的实际截图（ER图、页面截图、sqlmap扫描结果截图等）建议后期手动补上
4. 配色参考：主色 #2D5016（深绿），辅色 #8B5E3C（棕色），背景白色
================================================================================
